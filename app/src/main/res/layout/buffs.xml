<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
	android:orientation="vertical">

	<include
		android:id="@+id/mainToolbar"
		layout="@layout/main_toolbar"
		android:layout_width="match_parent"/>

	<LinearLayout
		android:orientation="vertical"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:gravity="center">

		<Button
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:id="@+id/buffsNewBuffButton"
			android:text="New Buff"
			style="?android:attr/buttonBarButtonStyle"
			android:padding="25dp"
			android:onClick="onNewBuffButtonClick"/>

		<Button
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:text="Manage Buffs"
			android:id="@+id/buffsManageButton"
			android:padding="25dp"
			style="?android:attr/buttonBarButtonStyle"
			android:onClick="onManageBuffsButtonClick"/>

		<Button
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:text="Manage Favorites"
			android:padding="25dp"
			style="?android:attr/buttonBarButtonStyle"
			android:onClick="onManageFavoritesButtonClick"
			android:id="@+id/buffsManageFavoritesButton"/>

	</LinearLayout>

</LinearLayout>

